package oovu.clients;

import com.cycling74.max.Atom;

abstract public class LazyServerClient extends ModuleMemberServerClient {

    public LazyServerClient(Atom[] arguments) {
        super(arguments);
    }
}
